import React, { Component } from 'react'
import classNames from 'classnames'
import styles from './DividingLine.scss'

const DividingLine = (props) => {
    return <span className={styles.border}></span>
}

export default DividingLine
